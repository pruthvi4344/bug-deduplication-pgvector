import {Moon,Sun,Search,Upload,BarChart3} from 'lucide-react';
type Page='home'|'search'|'benchmark'|'about'; export function Navbar({page,setPage,dark,setDark}:{page:Page;setPage:(p:Page)=>void;dark:boolean;setDark:(v:boolean)=>void}){return <header className="border-b border-slate-200 dark:border-slate-700"><nav className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><button onClick={()=>setPage('home')} className="text-lg font-bold text-indigo-500">BugVector AI</button><div className="flex items-center gap-1">{([['search','Search',Search],['benchmark','Benchmarks',BarChart3],['about','About',Upload]] as const).map(([p,l,I])=><button key={p} onClick={()=>setPage(p)} className={'rounded-lg px-3 py-2 text-sm '+(page===p?'bg-indigo-100 text-indigo-700 dark:bg-indigo-950':'')}><I className="mr-1 inline h-4 w-4"/>{l}</button>)}<button aria-label="Toggle dark mode" onClick={()=>setDark(!dark)} className="ml-2 rounded-lg p-2">{dark?<Sun/>:<Moon/>}</button></div></nav></header>}
export function Badge({value}:{value:number}){let c=value>.8?'bg-emerald-100 text-emerald-700':value>.6?'bg-amber-100 text-amber-700':'bg-slate-100 text-slate-700';return <span className={'rounded-full px-2 py-1 text-xs font-bold '+c}>{(value*100).toFixed(1)}% match</span>}
export function Spinner(){return <div className="mx-auto h-8 w-8 animate-spin rounded-full border-4 border-indigo-200 border-t-indigo-600"/>}

